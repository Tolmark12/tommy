/*
I'm thinking these classes will have a very close connection to their view. ie:
the manifestation which allows one to edit the value
ex: the drop down menu for a set of sizes
ex: the set of icons in Colors, etc...
*/

/* Probable vars required
height 
*/

/* Probable functions required
getValueString('red');
setValueString()			// returns 'red'
*/

/* Questions 1020firsts
1) How does the attribute's function modify the product, ex: if I change color, how do I connect the product view with the Control class?
2) Should each attribute only store ONE value? 
*/

interface lib.product.attribute.Attrbt
{
	function getValue():String;
	function setValue():String;
}